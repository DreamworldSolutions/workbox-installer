1. Observe service-worker's pre-caching behaviour in context of load error: Network Error (Timeout), Server Error (502, 503 or 504).
2. Observer workbox build behaviour in context of file names and what is being updated (re-downloaded) when a new version is released; and change was applied to only 1 fragment.
3. rollup workbox-build: integrate integrity feature if supported; and observe it's impact on workbox's precache configuration generated by it; and any import statements in ES modules.
4. Try reproducing the issue when service worker is installed for the first time; and new version of PWA is released.